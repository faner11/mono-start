{
  "name": "@repo/server",
  "version": "0.0.1",
  "description": "",
  "type": "module",
  "types": "types/src/routers/index.d.ts",
  "imports": {
    "#util": "./src/util/index.ts",
    "#comm": "./src/comm/index.ts"
  },
  "scripts": {
    "dev": "tsdown -w --onSuccess 'tsc -b && node --enable-source-maps dist/main.js'",
    "build": "tsgo -b && tsdown",
    "build:type": "tsgo -b tsconfig.build.json",
    "lint": "eslint .",
    "check-type": "tsgo -b --noEmit"
  },
  "dependencies": {
    "@hono/node-server": "^2.0.4",
    "@orpc/server": "^1.14.5",
    "@repo/db": "workspace:*",
    "@tanstack/query-core": "^5.101.0",
    "better-auth": "^1.6.15",
    "drizzle-orm": "^0.45.2",
    "hono": "^4.12.25"
  },
  "devDependencies": {
    "@types/pg": "^8.20.0"
  }
}
