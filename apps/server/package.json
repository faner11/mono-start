{
  "name": "@repo/server",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "tsdown -w --onSuccess 'tsc -b && node --enable-source-maps dist/main.js'",
    "build": "tsc -b && tsdown",
    "build:type": "tsc -b tsconfig.build.json",
    "lint": "eslint .",
    "check-type": "tsc -b --noEmit"
  },
  "description": "",
  "imports": {
    "#util": "./src/util/index.ts",
    "#comm": "./src/comm/index.ts"
  },
  "types": "types/src/routers/index.d.ts",
  "dependencies": {
    "@hono/node-server": "^1.19.0",
    "@hono/trpc-server": "^0.4.0",
    "@repo/db": "workspace:*",
    "@tanstack/query-core": "^5.85.3",
    "@trpc/server": "^11.4.4",
    "better-auth": "^1.3.7",
    "hono": "^4.9.2"
  }
}
