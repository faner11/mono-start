{
  "name": "@repo/db",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "db:push": "drizzle-kit push",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "lint": "eslint .",
    "check-type": "tsc -b"
  },
  "main": "./src/index.ts",
  "devDependencies": {
    "drizzle-kit": "^0.31.4"
  },
  "dependencies": {
    "drizzle-orm": "^0.44.5"
  }
}
